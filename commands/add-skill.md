# /add-skill - 스킬 추가

현재 역할에 맞는 추천 스킬을 보여주고 선택한 스킬을 추가합니다.

## 실행 방법
`/add-skill` 또는 `/add-skill [스킬명]`

## 진행 절차
1. 현재 역할 확인 (`~/.claude/CLAUDE.md`)
2. 역할별 추천 스킬 목록 표시 (이미 설치된 스킬은 체크 표시)
3. 사용자가 스킬 선택
4. 선택한 스킬의 SKILL.md를 `~/.claude/skills/` 에 복사
5. CLAUDE.md에 스킬 등록

## Phase 2에서 구현
이 커맨드는 Phase 2에서 완전히 구현됩니다.
